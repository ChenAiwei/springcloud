spring:
  application:
    name: eureka-client
  profiles:
    active: test
  security:
    user:
      name: root
      password: root

info:
  app:
    name: ${spring.application.name}
  company:
    name: test.com
  build:
    artifactId: $project.artifactId$
    version: $project.version$